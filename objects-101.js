//an object is an entity with a key and type
let myBook ={
    title: 'Harry Potter',
    author: 'JK Rowling',
    pageCount: 600,
};

console.log(`${myBook.title} is by ${myBook.author} and has ${myBook.pageCount} pages`);

let myData = {
    name : 'Neema',
    city: 'Kisuumu',
};
console.log(`${myData.name} lives in ${myData.city}`)
